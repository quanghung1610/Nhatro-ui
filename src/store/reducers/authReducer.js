const initState = {
    isLoggedIn: false,
    token: null
}

const authRedux = (state = initState, action) =>{
    switch (action.type) {
        
    
        default:
            return state;
    }
}

export default authRedux;