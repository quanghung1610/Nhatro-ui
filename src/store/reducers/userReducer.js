const initState = {
    userData: {}
}

const userRedux = (state = initState, action) =>{
    switch (action.type) {
        
    
        default:
            return state;
    }
}

export default userRedux;